# SimuSp3 交付文档

SP3 精密星历轨道/钟差误差仿真工具

---

# 第一部分：怎么跑

## 1.1 这是什么

给 GNSS + LEO 联合精密 SP3 星历添加仿真的轨道误差（R/T/N 方向）和钟差误差，生成含误差的 SP3 文件，用于 PPP 仿真验证。

- **输入：** csp3 项目产出的无误差联合 SP3
- **输出：** 所有卫星叠加了仿真轨道/钟差误差的 SP3
- **误差量级：** 轨道约 3–5 cm RMS，钟差约 3–4 cm（等效距离）

## 1.2 前置条件

| 项目 | 要求 |
|------|------|
| MATLAB | `D:\matlab\bin\matlab.exe`（路径已硬编码，不用改） |
| csp3 产出 | 对应 DOY 的联合 SP3 已存在 `F:\LeoSingle\gnssdata\data\projects\csp3\output\` |
| Python | 3.x（无额外依赖） |

## 1.3 一条命令

```bash
cd F:\LeoSingle\simusp3
python run_simusp3.py 2025 314 315
```

| 参数 | 说明 |
|------|------|
| `年份` DOY起始 [DOY结束] | 必填 |
| `--force` | 覆盖已有输出 |

**示例：**

```bash
python run_simusp3.py 2025 314            # 单天
python run_simusp3.py 2025 314 320        # 多天 DOY 314-320
python run_simusp3.py 2025 314 315 --force  # 强制覆盖已有
```

## 1.4 手动操作（不用 Python 驱动时）

如果需要在 MATLAB 交互环境里直接跑（比如只处理一个文件、调试、或看报错堆栈）：

### A：直接调 batch_simusp3（推荐）

```matlab
cd F:\LeoSingle\simusp3
% 先确认输入文件路径
input_path  = 'F:/LeoSingle/gnssdata/data/projects/csp3/output/whu23724_obs_simu.sp3';
output_path = 'F:/LeoSingle/gnssdata/data/projects/simusp3/Cwhu23724_obs_simu.sp3';
batch_simusp3(input_path, output_path)
```

- 路径用**正斜杠**（MATLAB 对反斜杠敏感）
- 输入输出路径都用**绝对路径**，`batch_simusp3` 不依赖工作目录
- 输出文件会自动新建，不会破坏输入文件

### B：用旧版脚本（不推荐）

`simusp3.m` 和 `SimuSp3_16.m` 是旧版脚本，文件名硬编码在代码里：

```matlab
% simusp3.m 第 73-74 行：
insp3  = 'whu22924.sp3';     % ← 手动改成你要的输入文件
outsp3 = 'Cwhu22924.sp3';    % ← 手动改成你要的输出文件
```

改完后在 MATLAB 里直接 `run simusp3.m`。

**用之前必须注意：**
1. 把输入文件复制到 `simusp3` 项目目录下（因为脚本写的是相对路径，不搜 gnssdata）
2. 旧版脚本会在运行目录生成 `ephe.mat`、`SP3.mat` 等中间文件
3. 不支持 `--force` 跳过逻辑，需要自己删输出文件
4. 主要用于对比验证，不建议日常使用

## 1.5 输出怎么看

```
============================================================
  SP3 Error Simulation: 2025 DOY 314-315
============================================================

--- DOY 314 (2025-06-18, week 2372, wd 4) ---
  Input:  whu23724_obs_simu.sp3 (45.0 MB)
  Output: Cwhu23724_obs_simu.sp3
  Running MATLAB (~45s per file)...
  MATLAB done (43s)
  >>> Done: Cwhu23724_obs_simu.sp3 (45.1 MB)

--- DOY 315 (2025-06-19, week 2372, wd 5) ---
  Already exists: Cwhu23725_obs_simu.sp3 (45.1 MB) [skipped]

============================================================
  Done in 43s: 1 generated, 1 skipped, 0 missing input, 0 failed
============================================================
```

**看这两行就够了：**

| 看什么 | 含义 |
|--------|------|
| `>>> Done:` | 该 DOY 成功完成 |
| 底行 Summary | 总体统计（`0 failed` 就是全部成功） |

**下面几行可以完全无视：**

| 文字 | 为什么不用管 |
|------|-------------|
| `Running MATLAB (~45s per file)...` | 纯进度提示，不是错误 |
| `Already exists: ... [skipped]` | 已处理过的文件自动跳过，不是失败 |
| MATLAB 命令行里跳 `Warning:` | Lagrange 边界插值警告，不影响结果 |
| MATLAB 命令行里跳 `%f`/`%i` 行 | SP3-c 注释行被 stdout 混出，无害 |

## 1.6 常见的两个问题

### 问题 A：MISSING INPUT

```
WARNING: No input file found for DOY XXX
```

**原因：** csp3 还没生成这个 DOY 的联合 SP3。  
**解决：** 去 csp3 项目跑批处理，确认 `whu{周}{周内序号}_obs_simu.sp3` 出现在 output 目录。

### 问题 B：MATLAB FAILED

```
MATLAB FAILED (Xs): 错误信息
```

**快速排查：** 手动执行看完整报错

```matlab
cd F:\LeoSingle\simusp3
batch_simusp3('F:/LeoSingle/gnssdata/data/projects/csp3/output/whu23724_obs_simu.sp3', ...
              'F:/LeoSingle/gnssdata/data/projects/simusp3/Cwhu23724_obs_simu.sp3')
```

常见的原因和解决见 4.2 节。

---

# 第二部分：跑出什么

## 2.1 文件位置

```
F:\LeoSingle\gnssdata\data\projects\simusp3\Cwhu{周}{周内序号}_obs_simu.sp3
```

示例：`Cwhu23724_obs_simu.sp3` = GPS 周 2372、周内序号 4（星期三）、所有卫星含误差。

## 2.2 文件规格

| 属性 | 值 |
|------|-----|
| 格式 | SP3-c |
| 卫星 | 全部 GNSS + LEO，每颗都加了误差（V1 模式） |
| 采样间隔 | 30 秒 |
| 文件大小 | ~45 MB |
| epoch 数 | ~2880（24 小时） |

## 2.3 单位链

**位置（容易混淆，注意看）：**

| 环节 | 实际值 | 操作 |
|------|--------|------|
| SP3 文件里 | km | ×1000 → |
| 内部计算 | **m**（变量名 `_mm` 是历史遗留，其实是米） | 叠加误差 |
| 写回 SP3 | km | /1000 |

**钟差：**

| 环节 | 实际值 | 操作 |
|------|--------|------|
| SP3 文件里 | μs | ×1e-6 → |
| 内部计算 | s | 叠加 dC(m) / c |
| 写回 SP3 | μs | ×1e6 |

**改参数时特别注意：** `r_amp_avg = 0.030` 的单位是**米**（即 30 mm），不是 0.03 mm。

## 2.4 下游消费者

产出文件直接给 `obs_simu_v2.0` 用：

```bash
cp Cwhu23724_obs_simu.sp3 F:/LeoSingle/obs_simu_data/
```

obs_simu 吃 CSV 格式，需先转。但那是 obs_simu 项目的事情，本工具只管产出标准 SP3-c。

## 2.5 验证方法

```matlab
cd F:\LeoSingle\simusp3
verify_simu('whu23724_obs_simu.sp3', 'Cwhu23724_obs_simu.sp3')
```

正常结果（各系统 RMS 应在预期范围）：

```
===== Summary by system (RMS, meters) =====
Sys    Count       dX       dY       dZ     dClk
GPS       30   0.0334   0.0324   0.0331   0.0325
GLO       18   0.0321   0.0313   0.0329   0.0310
GAL       24   0.0330   0.0328   0.0335   0.0318
BDS       39   0.0332   0.0331   0.0334   0.0320
LEO      150   0.0328   0.0330   0.0332   0.0314
```

- 三维 RMS ≈ 33 mm（参数设的 R=30, T=50, N=40 mm，>30 因为含余弦趋势）
- 钟差 RMS ≈ 31–32 mm
- 不会出现 NaN

## 2.6 性能

| 事项 | 时间（每文件） |
|------|---------------|
| MATLAB 冷启动 | ~15–25 s |
| 读取 + 算速度 | ~22 s |
| 仿真循环 | ~1 s |
| 写入 SP3 | ~22 s |
| **合计** | **~45 s** |

使用 `-batch` 模式（无 GUI）最快。

---

=======================================================================
**日常使用者看到这里就够了。以下为维护参考，改配置、排错、或想理解原理时再往下翻。**
=======================================================================

---

# 第三部分：工作原理

## 3.1 核心算法流程

```
输入 SP3（无误差）
     ↓
readsp3.m 读取 → sp3p 结构体（位置 xyz 单位 m，钟差单位 s）
     ↓
sp3p2sp3v.m 计算速度 → sp3v 结构体（9 阶 Lagrange 插值）
     ↓
对每颗有效卫星:
  1. selconf → 确定 SISRE 权重、轨道周期、系统类型
  2. simuar2 × 4 次 → 生成 R/T/N/C 四组误差序列
  3. ECEF → ECI（坐标转换）
  4. 在 ECI 中构建 RTN 基向量
  5. 将 RTN 误差叠加到轨道位置
  6. ECI → ECEF（转回地球固定系）
  7. 钟差叠加: clk_new = clk_old + dC/c
     ↓
writesp3.m 写入 → 含误差的 SP3 文件
```

## 3.2 误差模型

每颗卫星的每个方向独立仿真：

```
error(t) = A·cos(ωt + φ) + d + AR(2) 噪声
```

| 符号 | 含义 | 说明 |
|------|------|------|
| A | 振幅 | 全局均值 + 高斯随机偏差（每颗卫星不同） |
| ω = 2π/T | 角频率 | T = 轨道周期（由卫星高度算出） |
| φ | 初相位 | 0~2π 均匀随机 |
| d | 常数偏移 | 高斯随机 |
| AR(2) | 有色噪声 | noise(i) = 0.6·noise(i-1) + 0.25·noise(i-2) + e(i) |

**当前参数：**

| 分量 | 振幅均值 | 振幅 σ | 白噪声 σ | 偏移 σ | 趋势周期 |
|------|---------|--------|---------|--------|---------|
| 径向 R | 3.0 cm | 0.3 cm | 1.0 cm | 0.5 cm | T（轨道周期） |
| 切向 T | 5.0 cm | 0.3 cm | 1.0 cm | 2.0 cm | T |
| 法向 N | 4.0 cm | 0.3 cm | 1.0 cm | 1.0 cm | T |
| 钟差 C | 4.0 cm | 0.3 cm | 0.6 cm | 1.5 cm | **T/2**（半轨道周期） |

> 钟差故意用 T/2（不是 T），这是有意的设计选择——模拟径向与钟差的相关性。

## 3.3 V1 vs V2

| 特性 | V1（当前在用） | V2（旧版） |
|------|---------------|-----------|
| 入口 | `batch_simusp3.m` | `SimuSp3_16.m` |
| 加误差范围 | 全部卫星 | 仅 LEO（j ≥ 173） |
| GNSS 卫星 | 加误差 | 保持原值 |
| 钟差周期 | T/2（硬编码） | T（注释中有 T/2 但未启用） |
| 输出前缀 | `C` | `CGL` |
| 调用方式 | Python 驱动 | 手动改文件名后运行 |
| 用途 | 全系统误差仿真 | 仅 LEO 加误差，GNSS 用 WUM 精密产品 |

**当前 `run_simusp3.py` 固定调用 V1 行为（batch_simusp3.m），全部卫星都加误差。**

## 3.4 坐标转换与已知局限

坐标转换使用 Vallado 算法（IAU-1980），包含岁差、章动、恒星时、极移四步。

**Earth Orientation Parameters（EOP）硬编码为 2019-03-31 的值：**

```matlab
xp      = 187693e-6;   % arcsec
yp      = 206272e-6;   % arcsec
lod     = -4047e-7;    % s
ut1_utc = 122353e-7;   % s
```

**为什么这样做没问题：** EOP 仅用于构建 RTN 基向量。RTN 误差量级为 cm，EOP 误差导致的坐标转换偏差远小于 cm 级，不影响最终误差统计。

如果要改成真值，去 IERS 官网查对应日期的 Bulletin A。

## 3.5 卫星编号对照

| 系统 | 内部编号 | SP3 标识 | 数量 |
|------|---------|---------|------|
| GPS | 1–32 | G01–G32 | 32 |
| GLONASS | 33–59 | R01–R27 | 27 |
| Galileo | 60–111 | E01–E52 | 52 |
| BDS | 112–172 | C01–C61 | 61 |
| LEO | 173–322 | 201–350 | 150 |

BDS 子类型判断（`selconf.m` 第 48 行）：

```
PRN ≤ 10                                                    → GEO/IGSO (w_r=0.99)
PRN 为 13/16/31/38/39/40/56/≥59                             → GEO/IGSO (w_r=0.99)
其余                                                         → MEO (w_r=0.98)
```

## 3.6 卫星有效性判断

`batch_simusp3.m` 第 117 行：取 `floor(NoEp/2)` epoch（≈ 正午 12:00），如果 x/y/z/clk 全为 NaN → 跳过。

对正常 SP3 文件（所有卫星所有 epoch 都有数据）有效。对片段式 SP3（部分卫星部分时间无数据）不适用。

## 3.7 随机数的可重复性设计

每个方向（R/T/N/C）有 3 个独立种子段：

```matlab
s = RandStream('mcg16807','Seed',10001);  % → 振幅随机
s = RandStream('mcg16807','Seed',20001);  % → 相位随机
s = RandStream('mcg16807','Seed',30001);  % → 偏移随机
```

每颗卫星在 `simuar2` 中有独立种子（`sat = j + MaxSat * 方向编号`）。

**效果：** 同一份代码、同一种子 → 永远产生完全相同的误差序列。这是实验室复刻实验的基石。

**代价：** 改任意一个种子 → 全部误差序列都会变 → 旧数据无法复刻。

---

# 第四部分：配置与维护

## 4.1 改配置前必须知道的硬约束

### H1: MJD 不是 GPS 周，不要手动算

`run_simusp3.py` 的 `doy_to_gps_week()` 已经自动转换。公式仅供参考（你不需要手算）：

```python
gps_week = (mjd - 44244) // 7
weekday  = (mjd - 44244) % 7
```

### H2: poleut1 行尾必须是 LF，不能是 CRLF

SP3-c 标准要求 `EOF` 行前的内容以 Unix 换行符结束。如果上游文件被 Windows 工具处理过变成 CRLF，`readsp3.m` 的 `fgetl` 可能吞掉字符导致解析错位。用 Notepad++ 或 `file` 命令检查换行符类型。

### H3: 输入输出 SP3 的起止时间必须一致

`writesp3.m` 通过 epoch 时间字符串匹配来对齐数据。如果输入输出起止时间不同（一个从 23:55 开始、一个从 00:00 开始），写入会错位。

当前 Python 驱动的流程已保证不会出现此问题。

### H4: 修改卫星数量时，5 个文件必须同步更新

```matlab
NsatGPS = 32;  NsatGLO = 27;  NsatGAL = 52;  NsatCMP = 61;  NsatLEO = 150;
```

这 5 个 `global` 变量在以下文件中都存在：

- `batch_simusp3.m`
- `readsp3.m`
- `writesp3.m`
- `selconf.m`
- `verify_simu.m`

少改一个就会报 `Index exceeds matrix dimensions`。

### H5: 随机数种子的三段式结构不可动

```matlab
s = RandStream('mcg16807','Seed',10001);  % 振幅
s = RandStream('mcg16807','Seed',20001);  % 相位
s = RandStream('mcg16807','Seed',30001);  % 偏移
```

每个方向（R/T/N/C）× 3 个种子段，加上 `simuar2` 里每颗卫星 `sat` 独立种子。**动任意一个，全部误差序列改变，已发表数据无法复刻。**

### H6: 内部单位是米，不是毫米

尽管变量名中有 `_mm` 后缀（历史遗留），实际单位是米：

| 变量 | 实际单位 | 说明 |
|------|---------|------|
| `sp3p.recef(:,1:3,:)` | m | 读入时 ×1000 → 米 |
| `r_amp_avg = 0.030` | m（即 30 mm） | 误差参数 |
| `recef_mm` | **m**（变量名是错的） | 代码注释已标注 |

## 4.2 完整错误排查

### 4.2.1 假警报速查表

| 看到这个 | 它其实是 | 怎么办 |
|---------|---------|-------|
| `Running MATLAB (~45s per file)...` | 进度提示 | 等着 |
| `Already exists: xxx [skipped]` | 自动跳过已处理文件 | 要重跑就加 `--force` |
| MATLAB `Warning:` 输出 | Lagrange 边界插值 / 矩阵近似奇异 | 无视 |
| 命令行里跳出 `%f` `%i` 行 | SP3-c 注释被 stdout 带出 | 无害 |

### 4.2.2 真实错误及处理

#### E1: MATLAB FAILED

```
MATLAB FAILED (Xs): ...
```

| 错误消息具体内容 | 原因 | 解决 |
|----------------|------|------|
| `Input file not found:` | MATLAB 里路径不对 | Python 已做 `replace(chr(92), '/')`，通常不会出现。检查路径中有无中文字符 |
| `SP3 file error:` | 文件损坏或被占用 | 检查文件大小是否 ~45 MB，重跑 |
| `readsp3: no epoch reference time found` | SP3 头部 `#` 行解析失败 | 检查 SP3 文件第一行格式 |
| `Index exceeds matrix dimensions` | 卫星数配置与实际不符 | `MaxSat=322` 和实际 SP3 卫星数对不上 |
| 中文乱码或 MATLAB 直接崩 | `sscanf('  ','%d')` 触发了 MATLAB 的 bug | 检查读取的 SP3 行尾是否有空白行 |

**通用排查：** 在 MATLAB 里手动调一次，能看到完整堆栈。

#### E2: MISSING INPUT

```
WARNING: No input file found for DOY XXX
```

**原因：** csp3 还没生成该 DOY 的文件。  
**流程：**

1. 确认该 DOY 对应的 GPS 周（脚本会自动算，无需手算）
2. 去 csp3 项目跑批处理
3. 确认 `whu{周}{周内序号}_obs_simu.sp3` 在 `F:\LeoSingle\gnssdata\data\projects\csp3\output\` 里

#### E3: 输出文件大小为 0 或远小于 45 MB

**原因：** MATLAB 写入被中断（超时、手动关窗）  
**解决：** 删掉坏文件 → `--force` 重跑

#### E4: 程序卡住超过 120 秒

- 首次冷启动 MATLAB 较慢（~15–25 s），正常
- 始终无响应 → 任务管理器杀掉 MATLAB 进程 → 重跑

## 4.3 文件全景

```
F:\LeoSingle\simusp3/
├── run_simusp3.py       [入口] 日常只用这个
├── batch_simusp3.m      [核心] MATLAB 批处理函数
│
├── simusp3.m            旧版 V1 主脚本（硬编码文件名，已不直接用）
├── SimuSp3_16.m         旧版 V2 主脚本（仅 LEO 加误差）
│
├── readsp3.m            读 SP3
├── writesp3.m           写 SP3
├── sp3p2sp3v.m          位置→速度
├── simuar2.m            误差仿真
├── selconf.m            卫星配置
├── verify_simu.m        验证
├── plotephe.m           可视化
│
├── ecef2eci.m           坐标转换（ECEF → ECI）
├── eci2ecef.m           坐标转换（ECI → ECEF）
├── precess.m            岁差
├── nutation.m           章动
├── sidereal.m           恒星时
├── polarm.m             极移
├── fundarg.m            天文参数
├── gmstime.m            格林威治恒星时
├── iau80in.m            IAU 1980 系数加载
├── lag.m                10 阶 Lagrange 插值
├── nut80.dat            章动系数表
│
└── 交付文档.md          本文档
```

## 4.4 核心调用链

```
run_simusp3.py
  ├── doy_to_gps_week()       # 年份+DOY → GPS 周 + 周内序号
  ├── find_input_file()       # 搜 csp3 产出目录
  └── run_matlab()            # 调用 MATLAB
       └── batch_simusp3.m
            ├── 预计算旋转矩阵 R（EOP 固定，所有 epoch 共享）
            ├── 初始化随机种子（一次性，所有卫星共享）
            ├── readsp3()        → sp3p 结构体
            ├── sp3p2sp3v()      → sp3v 结构体
            ├── 卫星循环 for j=1:MaxSat
            │     ├── simuar2() ×4       → R/T/N/C 误差
            │     ├── ECEF → ECI          → RTN 基向量构建
            │     ├── 向量化叠加误差       → 对应所有 epoch 一次性矩阵运算
            │     └── ECI → ECEF          → 写回 sp3p.recefe
            └── writesp3()       → 输出含误差 SP3
```

## 4.5 处理新 DOY 的完整流程

```
1. 确认 csp3 已生成 whu{周}{周内序号}_obs_simu.sp3
   → 在 F:\LeoSingle\gnssdata\data\projects\csp3\output\ 检查

2. python run_simusp3.py <年份> <DOY起始> [DOY结束]

3. 验证输出文件大小 ≈ 45 MB

4. （可选）用 verify_simu 抽查差异 RMS

5. 更新 README：
   F:\LeoSingle\gnssdata\data\projects\simusp3\README.md
   追加"已有数据"表的新行
```

## 4.6 已知踩坑备忘录

1. **上游 csp3 重跑后必须重跑 simusp3。** csp3 的 output 目录被覆盖时，simusp3 的归档不会自动同步。

2. **随机种子不可逆。** 误差参数或种子值一旦改过，旧输出的 SP3 无法用新代码复刻。需要复刻就切回改参数前的 git commit。

3. **MATLAB 有一个崩溃 bug：** `sscanf('  ','%d')` 在空白输入上会让 MATLAB 直接崩溃（不是返回空，是 crash）。所有 sscanf 前必须有 `length(tline) >= N` 守卫。当前代码已全部加好，新增读取逻辑时必须注意。

4. **不要用 `fclose('all')`。** 会关闭 MATLAB 进程所有文件句柄，已踩过坑。

5. **删除文件前必须 grep 全项目确认无调用。** 例：`gmstime.m` 被 `sidereal.m` 第 52 行调用，但只看文件名列表发现不了间接依赖。应当 `grep -r "gmstime" *.m`。

---

*文档版本: 2026-07-20*
*交付人: LeoSingle*
